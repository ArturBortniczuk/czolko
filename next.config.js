/** @type {import('next').NextConfig} */
const nextConfig = {
  // Usuń export output podczas debugowania
  // output: 'export',
  // trailingSlash: true,
  // images: {
  //   unoptimized: true
  // }
}

module.exports = nextConfig